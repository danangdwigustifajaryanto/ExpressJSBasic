<!DOCTYPE html>
<html lang="id" class="bg-white dark:bg-zinc-900">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tambah Buku</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-white dark:bg-zinc-900 text-zinc-900 dark:text-white min-h-screen p-6">
  <%- include('layout/sidebar') %>
  <div class="max-w-2xl mx-auto bg-white dark:bg-zinc-800 p-6 rounded-xl shadow-md">
    <h2 class="text-xl font-semibold mb-4">Tambah Buku</h2>
    <form action="/buku/tambah" method="POST" enctype="multipart/form-data" class="space-y-4">
      <div>
        <label for="judul" class="block mb-1">Judul</label>
        <input type="text" id="judul" name="judul" required class="w-full px-4 py-2 bg-zinc-100 dark:bg-zinc-700 rounded-md">
      </div>
      <div>
        <label for="pengarang" class="block mb-1">Pengarang</label>
        <input type="text" id="pengarang" name="pengarang" required class="w-full px-4 py-2 bg-zinc-100 dark:bg-zinc-700 rounded-md">
      </div>
      <div>
        <label for="penerbit" class="block mb-1">Penerbit</label>
        <input type="text" id="penerbit" name="penerbit" required class="w-full px-4 py-2 bg-zinc-100 dark:bg-zinc-700 rounded-md">
      </div>
      <div>
        <label for="stok" class="block mb-1">Stok</label>
        <input type="number" id="stok" name="stok" required class="w-full px-4 py-2 bg-zinc-100 dark:bg-zinc-700 rounded-md">
      </div>
      <div>
        <label for="foto" class="block mb-1">Foto</label>
        <input type="file" id="foto" name="foto" required class="w-full">
      </div>
      <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-md">Tambah</button>
    </form>
  </div>
  <button onclick="document.documentElement.classList.toggle('dark')" class="fixed bottom-4 right-4 w-10 h-10 rounded-full bg-zinc-800 text-white flex items-center justify-center shadow-lg">🌓</button>
</body>
</html>